// Override animation / interaction settings without touching script.js.
// Uncomment the properties you want to tweak.
window.siteFX = window.siteFX || {
  // enableAurora: false,
  // enableMagnet: false,
  // magnetStrength: 14,
  // navThreshold: 0.35
};
